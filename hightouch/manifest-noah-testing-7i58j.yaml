# READ ONLY FILE
# Changes made here will be ignored. Please use the Hightouch dashboard to create and edit sources and destinations.

sources:
  google-sheets:
    name: Google Sheets
    type: googlesheets
  postgre-sql-localhost:
    name: PostgreSQL localhost
    type: postgres
destinations:
  http-request-testing:
    name: HTTP Request testing
    type: webhookV2
